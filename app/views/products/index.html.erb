<p id="notice"><%= notice %></p>

<br>
<h1 class="product-header">Products</h1><br><br>

<div class="container_fluid">
  <div class="row">
    <% @products.each do |product| %>
      <div class="col-sm-2 col-md-4 product-style">

        <%= image_tag(product.image_url, class:"img-responsive") %>

        <a href="#"><h2><%= product.name %></h2>
        <p><%= product.description %></p>
        <p>Type of Food: <%= product.food_type %></p>
        <p><strong>Price: <%= product.price %></strong> €</p>
        <br>
        <p><%= link_to 'Show', product %>
        <%= link_to 'Edit', edit_product_path(product) %>
        <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %> </p> <br> <br>
      </div> <!-- end column -->
    <% end %>
  </div>  <!-- end Row -->
</div>  <!-- end Container -->


<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Image url</th>
      <th>Food type</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td><%= product.image_url %></td>
        <td><%= product.food_type %></td>
        <td><%= product.price %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        
      </tr>
    <% end %>
  </tbody>
</table> -->

<br>

<%= link_to 'New Product', new_product_path %>